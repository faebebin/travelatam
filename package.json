{
  "name": "travelatam",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "start-dev": "VITE_API=dev vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^8.31.0",
    "eslint-plugin-import": "^2.26.0",
    "gh-pages": "^4.0.0",
    "vite": "^3.0.9",
    "vitest": "^0.24.0"
  },
  "dependencies": {
    "nosleep.js": "^0.12.0",
    "ol": "latest"
  }
}
